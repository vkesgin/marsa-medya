<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /icerikyonetimi/
  
  # If the requested filename is not a directory or file, route to index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [QSA,L]
</IfModule>
